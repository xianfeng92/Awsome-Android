# systrace

In order to run systrace, complete the following steps:

1. From Android Studio, download and install the latest Android SDK Tools.

2. Install Python and include it in your workstation's execution path.

3. Connect a device running Android 4.3 (API Level 18) or higher to your development system using a USB debugging connection.

The systrace tool is provided in the Android SDK Tools package and is located in android-sdk/platform-tools/systrace/.


To generate the HTML report for app, you need to run systrace from the command line using the following syntax:

```
python systrace.py [options] [categories]

```










































[systrace](https://developer.android.com/studio/command-line/systrace)
